apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The real Secret is created out-of-band (see secret.example.yaml) and is NOT listed here.
resources:
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
  - prometheusrule.yaml

images:
  - name: ghcr.io/your-org/compliance-dvn-worker
    newTag: latest

commonLabels:
  app.kubernetes.io/part-of: compliance-dvn
