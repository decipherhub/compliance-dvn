node_modules
.env
coverage
coverage.json
typechain
typechain-types

# Hardhat files
cache
artifacts


# LayerZero specific files
.layerzero

# foundry test compilation files
out

# pnpm
pnpm-error.log

# Editor and OS files
.DS_Store
.idea

# internal process docs (design spec + plan)
docs/superpowers/

# presentation deck — kept local, not uploaded
presentation/

# worker runtime state (scan checkpoint, deferred queue, approvals) — per-deployment, not source
worker/.context/
